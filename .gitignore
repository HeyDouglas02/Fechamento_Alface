# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Banco local (sql.js) e backups
server/data
*.db
*.db.tmp
*.bak
*.bak_*

# (public/logo.ico é versionado: antes era gerado pelo criar-atalho.bat, que
#  não existe mais — sem ele o atalho da área de trabalho fica sem ícone.)

# Configuração local do Claude Code e artifacts de design (pessoal/tool, não versionar)
.claude/
.impeccable/

# Variáveis de ambiente
.env
.env.local

# Guia de instalação da loja — uso interno, não vai pro repositório público
docs/INSTALACAO.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
